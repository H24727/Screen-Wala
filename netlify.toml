[build]
  publish = "."
  functions = "netlify/functions"

# Without this, calls to /api/... never reach the functions folder —
# Netlify serves functions at /.netlify/functions/<name> by default.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
